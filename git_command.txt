   ##commands-of-gits
    
   git add [file_name]
   git status
   git rm --cached [file_name]
   git add .
   git commit -m "message"
   rm [file_name]
   ls
   git restore testing.py
 git remote -v
   git branch -M main
   git branch -M master
   git remote set-url origin (use token url)
        [e.g git remote set-url origin https://tokenurl@github.com/MdAdnanImteyaz/git-for-devops.git
   git push origin master

this is somenew changes 
